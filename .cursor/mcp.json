{
  "mcpServers": {
    "langchain-mcp":{
        "type": "stdio",
        // Links contains {workspaceFolder} which should be 
        // replaced with absolute path to the project.
        // Follow the `./mcp-server/instructions/cursor-mcp-settings.agent.md` instuctions.
        "command": "{workspaceFolder}\\.virtualenv\\Scripts\\python.exe",
        "args": ["{workspaceFolder}\\mcp_server\\server.py"]
    }
  }
}